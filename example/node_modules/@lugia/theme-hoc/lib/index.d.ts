/**
 * 组件样式处理增强
 * @flow
 */
import { ThemeHocOption } from './type';
import React from 'react';
export { addFocusBlurEvent, addMouseEvent } from '@lugia/theme-core';
declare type ThemeHocProps = {
    lugiaHidden?: boolean;
    widgetId?: string;
    viewClass?: string;
};
declare const ThemeProvider: (Target: React.ComponentType<any>, widgetName: string, opt?: ThemeHocOption) => React.ForwardRefExoticComponent<ThemeHocProps>;
export default ThemeProvider;
